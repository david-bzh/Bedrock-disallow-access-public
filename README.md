# Bedrock-disallow-access-public
This plugin will prevent access to the site for non-logged in users when `WP_ENV` is set to `staging`.

## Install

```
composer require david-bzh/bedrock-disallow-access-public
```
